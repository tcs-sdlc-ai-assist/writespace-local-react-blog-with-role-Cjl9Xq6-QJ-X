# WriteSpace Environment Variables
# No environment variables are required — all data is stored in localStorage.

# Optional: Customize the app title displayed in the browser tab
# VITE_APP_TITLE=WriteSpace